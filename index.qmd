---
title: "My First Quarto Site"
author: "Your Name"
format: html
---

## Welcome

This page was written in **Quarto** and published straight from GitHub.

-   It renders to a real web page.
-   The source is plain text, so Git tracks every change cleanly.

```{python}
print("Hello from a rendered code chunk!")
```

```{python}
#| echo: false

print("This line was produced by Python when the document was rendered — the code that made it is hidden.")
```

**Dataset:** `campus_cafe_sales.csv` (posted with this assignment). 90 rows of daily sales for three campus cafés over six weekdays.

```{python}
#| include: false

# Runs silently. Nothing appears here, but `sales` exists for every chunk below.
import pandas as pd
sales = pd.read_csv("data/campus_cafe_sales.csv")
```

## Load

```{python}
import pandas as pd
import plotly.express as px

sales = pd.read_csv("data/campus_cafe_sales.csv")
sales.head()
```

## Inspect

```{python}
print("Rows and columns:", sales.shape)
print("Cafes:", sales["cafe"].unique())
sales.dtypes
```

## Calculate Revenue and Filter by Coffee

```{python}
sales["revenue"] = sales["units_sold"] * sales["unit_price"]

coffee = sales[sales["category"] == "Coffee"]
coffee.head()
```

## Plot

```{python}
daily = coffee.groupby(["date", "cafe"], as_index=False)["revenue"].sum()

px.line(daily, x="date", y="revenue", color="cafe", markers=True,
        title="Daily coffee revenue by cafe",
        labels={"date": "Date", "revenue": "Revenue ($)"})
```

## Callout 

        
::: {.callout-note}
## Why I filtered to coffee

Coffee is two of the five items but most of the volume, so a revenue
trend across all categories mostly just shows the coffee trend anyway.
:::
